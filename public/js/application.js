$(document).ready(function(){
  // Set debug mode (for console logs)
  LayerHandler.debug = true;

  LayerHandler.data.gtm_id = 'YOUR_GTM_ID_HERE';

  // Executes the testMethod
  LayerHandler.testMethod();
});
